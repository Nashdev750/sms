<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0">
                    <i class="bi bi-person-plus me-2"></i>
                    Add New Student
                </h5>
            </div>
            <div class="card-body">
                <form method="POST" action="/students/add">
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label for="admission_no" class="form-label">Admission Number</label>
                            <input type="text" class="form-control" id="admission_no" name="admission_no" 
                                   value="<%= formData ? formData.admission_no : '' %>" required>
                            <% if (errors && errors.find(e => e.param === 'admission_no')) { %>
                                <div class="text-danger small mt-1">
                                    <%= errors.find(e => e.param === 'admission_no').msg %>
                                </div>
                            <% } %>
                        </div>
                        
                        <div class="col-md-6 mb-3">
                            <label for="name" class="form-label">Full Name</label>
                            <input type="text" class="form-control" id="name" name="name" 
                                   value="<%= formData ? formData.name : '' %>" required>
                            <% if (errors && errors.find(e => e.param === 'name')) { %>
                                <div class="text-danger small mt-1">
                                    <%= errors.find(e => e.param === 'name').msg %>
                                </div>
                            <% } %>
                        </div>
                    </div>
                    
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label for="class_level" class="form-label">Class Level</label>
                            <select class="form-select" id="class_level" name="class_level" required>
                                <option value="">Select Class</option>
                                <% classLevels.forEach(function(classLevel) { %>
                                    <option value="<%= classLevel %>" 
                                            <%= formData && formData.class_level === classLevel ? 'selected' : '' %>>
                                        Grade <%= classLevel %>
                                    </option>
                                <% }); %>
                            </select>
                            <% if (errors && errors.find(e => e.param === 'class_level')) { %>
                                <div class="text-danger small mt-1">
                                    <%= errors.find(e => e.param === 'class_level').msg %>
                                </div>
                            <% } %>
                        </div>
                    </div>
                    
                    <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                        <a href="/students" class="btn btn-outline-secondary me-md-2">
                            <i class="bi bi-arrow-left me-2"></i>
                            Cancel
                        </a>
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-check-lg me-2"></i>
                            Add Student
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<div class="row mt-4">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <h6 class="card-title">
                    <i class="bi bi-info-circle me-2"></i>
                    Student Information Guidelines
                </h6>
                <ul class="list-unstyled mb-0">
                    <li><i class="bi bi-check-circle text-success me-2"></i>Admission number must be unique</li>
                    <li><i class="bi bi-check-circle text-success me-2"></i>Use the student's full legal name</li>
                    <li><i class="bi bi-check-circle text-success me-2"></i>Select the appropriate class level (7, 8, or 9)</li>
                    <li><i class="bi bi-check-circle text-success me-2"></i>Student can be moved to different classes later</li>
                </ul>
            </div>
        </div>
    </div>
</div>
