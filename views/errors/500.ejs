<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Junior Secondary Grading System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container">
        <div class="row justify-content-center align-items-center min-vh-100">
            <div class="col-md-6 text-center">
                <div class="card border-0 shadow">
                    <div class="card-body p-5">
                        <i class="bi bi-exclamation-octagon display-1 text-danger mb-4"></i>
                        <h1 class="display-4 fw-bold text-danger">500</h1>
                        <h2 class="h4 mb-3"><%= title %></h2>
                        <p class="text-muted mb-4"><%= message %></p>
                        <% if (error && error.stack) { %>
                            <details class="text-start mb-4">
                                <summary class="btn btn-outline-secondary btn-sm">Show Error Details</summary>
                                <pre class="mt-2 p-3 bg-light border rounded small"><%= error.stack %></pre>
                            </details>
                        <% } %>
                        <a href="/dashboard" class="btn btn-primary">
                            <i class="bi bi-house me-2"></i>
                            Go to Dashboard
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
